# Regular package marker so Pylance can resolve the algorithm.* imports
# unambiguously (the sibling variant directories also ship an `algorithm`
# package, and without this file the four collide as namespace packages).
