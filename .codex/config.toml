model = "deepseek-v4-flash"
preferred_auth_method = "apikey"
forced_login_method = "api"
model_reasoning_effort = "high"
model_catalog_json = "/home/tuantb/.codex/models.json"

# NOTE: model_provider and model_providers are NOT supported at project level in
# codex 0.146 - they are ignored with a warning. The DeepSeek provider is defined
# in ~/.codex/config.toml. To run with DeepSeek, use:
#   codex -c model_provider=deepseek